/**
 * @grumeup/types
 *
 * Shared TypeScript types and interfaces used across the monorepo.
 * Add domain types, API contracts, and shared interfaces here.
 */

// Placeholder — types will be added as features are built
export type {};
